# Agent Skill 编写实战指南

> 基于《Agent Skill Authoring Specification》提炼的实操手册。面向想为自己（AI Agent）编写高质量技能的开发者。

---

## 一、核心认知：Skill 不是什么

| 误解 ❌ | 真相 ✅ |
|---|---|
| Skill 是一份详细的操作说明书 | Skill 是**结构化行为上下文补丁** |
| Skill 是像传统代码一样去写 | Skill 是像**设计提示词**一样去设计 |
| Skill 内容越多越好 | **每个 Skill 都是一种税**——多余 token = 成本 ↑ + 注意力分散 ↑ |

**黄金法则**：每写一句话，问自己——*"如果没有这句话，Agent 真的会犯错吗？"* 如果不会，删掉。

---

## 二、目录结构标准（Hub-and-Spoke 架构）

每个 Skill 必须是一个**目录**，而非单个文件：

```
your-skill-name/          # 全小写，连字符分隔
├── SKILL.md              # 核心入口：元数据 + 高价值核心指令（< 5000 tokens）
├── scripts/              # 可直接运行的脚本（禁止让 Agent 现场发明代码）
├── references/           # 深度资料：白皮书、技术标准（按需加载）
├── assets/               # 模板、Schema 等静态资产
└── config.json           # 初始化配置
```

**多级嵌套原则**：复杂领域（如税法）建 2-3 层树状结构，引导模型"先定位大类 → 再锁定子项 → 最后精准读取"，严禁平铺。

---

## 三、三层上下文成本控制

```
Index 层 (~100 tokens)     ← 全局常驻，仅做路由触发
  │
  ▼ 路由命中 → load_skill
Load 层 (< 5,000 tokens)   ← SKILL.md 正文，条件加载
  │
  ▼ 运行时按需 Read
Runtime 层 (无上限)         ← scripts/ references/，用完即走
```

**设计原则**：
- **Index** = 名称 + 一行 description（极度精炼，仅用于触发）
- **Load** = 核心策略和边界条件（高价值，控制体积）
- **Runtime** = 深度知识（按需读取，不污染主上下文）

---

## 四、五步编写流程

### Step 0：先写评估集（EDD 模式）

动笔前先定义三类用例：

| 类型 | 作用 |
|---|---|
| **正样本** (Positive) | 最典型的用户查询场景 |
| **已知坏例** (Known Failures) | 缺此技能时 Agent 必然会答错的 case |
| **负样本** (Neighbor Confusion) | 语义邻近但**禁止触发**的场景 |

### Step 1：精炼 Description（路由触发器）

- ❌ 不要写人类友好的介绍：`"This skill helps user to..."`
- ✅ 写成路由模型看的隐式匹配器：`Load when [具体的用户意图或触发场景]`
- 控制在 **50 词以内**，信号密度越高越好

### Step 2：注入观点与人类品味

- 不要重复模型已知的常识（如标准 Git 用法）
- 注入**人类专家的主观判断**：审美标准、企业特有流程、非公开准则

### Step 3：资产剥离

写完 SKILL.md 正文后，检查：
- 有长篇表格？→ 挪到 `references/`
- 有 JSON Schema？→ 挪到 `assets/`
- 有大量边界 case？→ 挪到 `SPECIAL_CASES.md`

保持 SKILL.md 本身精炼。

### Step 4：Append-Mostly 维护

- 技能生命在于维护，遵循**只增不删**原则
- Agent 又犯错时 → 找根因 → 在尾部追加一条 **Gotcha**
- 不要大改主文件

---

## 五、反模式自查清单（每条都是 Red Flag）

写完后逐条检查，触发任意一条即为不合格：

- [ ] ❌ **单文件依赖**：把所有内容堆在一个 SKILL.md 里
- [ ] ❌ **轨道化常识**：教模型如何用标准 Linux/Git 命令（模型早会了）
- [ ] ❌ **描述废话**：`"This skill provides functions to..."` → 应改为触发场景
- [ ] ❌ **缺乏负样本**：没提供禁止触发场景，导致干扰其他技能（Action at a Distance）
- [ ] ❌ **硬编码敏感资产**：把频繁变动的 API 版本、MCP 工具写死在技能里

---

## 六、验证防线

产出后通过以下四层测试：

1. **路由测试**：混淆样本不得错误激活此 Skill
2. **渐进读取检查**：模型只在需要时才去读 `references/`
3. **LLM Judge 评估**：用最强模型对 Agent 输出进行质量打分
4. **跨模型回归**：在 GPT、Claude 等多个模型上跑通，无远距离干扰

---

## 七、一句话总结

> **Skill 是"如果缺了它 Agent 会犯错"的高价值上下文补丁，用目录结构渐进加载，用评估集保证精度，用 Append-Mostly 持续进化。多余的全删掉。**
